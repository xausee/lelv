// 此文件存放mongo命令，用来创建和配置数据库信息

use admin
// 创建超级管理员账号
db.createUser({
    user: "lelvAdmin",
    pwd: "lelvAdmin",
    roles: [
        {
            role: "root",
            db: "admin"
        }
    ]
})

use lelv
// 创建对数据库lelv的普通用户
db.createUser({
    user: "User",
    pwd: "000000",
    roles: [
        {
            role: "readWrite",
            db: "lelv"
        }
    ]
})